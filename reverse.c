#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv)
{
    if (argc > 3)
    {
        fprintf(stderr, "usage: reverse <input> <output>\n");
        exit(1);
    }

    // Funcionalidad para reverse
    return 0;
}